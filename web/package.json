{
  "name": "goyim-arena-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test \"test-build/tests/**/*.test.js\""
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
